@SERIAL_CONFIG@
@TERMINAL_CONFIG@

set default="0"
set timeout="@TIMEOUT@"

if [ x${grub_platform} = xefi ]; then
    set root='(cd0)'
else
    set root='(cd)'
fi

menuentry "@TITLE@" {
	linux /boot/vmlinuz @CMDLINE@ noinitrd
}
